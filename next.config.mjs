// next.config.mjs
/** @type {import('next').NextConfig} */
const nextConfig = {
  // O Next.js é mais rigoroso. Isso ajuda com bibliotecas como a 'recharts'.
  reactStrictMode: false, 
};

export default nextConfig;